# Launching Experiments on NGC

TODO

To attach the l5kit prediction dataset: `--datasetid 90893:/your/dataset/mounting/path/> ` 

To access a running job through command line, run: `ngc batch exec <job-ID>`